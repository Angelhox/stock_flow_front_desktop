import { IconType } from 'react-icons'

export interface IMenuItem {
  id: string
  label: string
  icon: IconType
  url: string
}
export interface IUSerCard {
  id: string
  displayName: string
  title: string
  photoUrl: string
  url: string
}
